Flask==3.0.0
flask-cors==4.0.0
notion-client==2.2.1
requests==2.31.0
gunicorn==21.2.0
```

**`Procfile`** (tells Railway how to run your app):
```
web: gunicorn app:app
```

# **`.env.example`** (don't commit actual keys):
# ```
# NOTION_API_KEY=your_key_here
# DATASOURCE_ID=your_datasource_id_here